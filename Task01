HTTP 1.1 :-
	It works in textual formats.
	There is head of line blocking that blocks all the requests behind it until it doesn’t get its all resources.
	It uses requests resource Inlining for use getting multiple pages
	It was created in 1997.
	It compresses data by itself.
	Some methods and response  codes are added.
	Syntax and Semantics are separated.

HTTP 2 :-
	It works in Binary protocol.
	It allows multiplexing so one TCP connection is required for multiple requests.
	It uses push frame by server that collects all multiple pages 
	It was created in 2015.
	It uses HPACK for data compression.
	Support of parallel request transmission by stream.
	Addition of server-push function.


Objects :-
  Object is a standalone entity having several properties and types. Compare this with a car as an object has a properties with colour, design, 
and its material made of it.
The syntax of creating object using object literal is given below:
          object = {property1: value1;property2: value2...propertyn: valuen}; 
